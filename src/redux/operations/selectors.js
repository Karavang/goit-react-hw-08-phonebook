export const selectIsLogged = state => state.regs.isLogged;

export const selectUser = state => state.regs.user;

export const selectIsLoading = state => state.regs.isLoading;

export const selectToken = state => state.regs.token;
